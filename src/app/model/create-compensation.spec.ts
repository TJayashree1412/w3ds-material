import { CreateCompensation } from './create-compensation';

describe('CreateCompensation', () => {
  it('should create an instance', () => {
    expect(new CreateCompensation()).toBeTruthy();
  });
});
